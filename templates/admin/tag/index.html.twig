{% extends 'base.html.twig' %}

{% block title %}Tags management{% endblock %}

{% block body %}
    <h1 class="mb-3">Tags management</h1>

    {% include 'partials/_flash_messages.html.twig' %}

    <a href="{{ path('app_tag_new') }}" class="btn btn-primary">Create new</a>

    <div class="table-responsive">
        <table class="table table-striped mt-3">
            <thead>
            <tr>
                <th class="text-nowrap">ID</th>
                <th class="text-nowrap">Name</th>
                <th class="text-nowrap">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for tag in pagination %}
                <tr>
                    <td class="text-nowrap">{{ tag.id }}</td>
                    <td class="text-nowrap">{{ tag.name }}</td>
                    <td class="d-flex align-items-center gap-2">
                        <a href="{{ path('app_tag_show', {'id': tag.id}) }}" class="btn btn-primary btn-sm">Show</a>
                        <a href="{{ path('app_tag_edit', {'id': tag.id}) }}" class="btn btn-warning btn-sm">Edit</a>
                        <form action="{{ path('app_tag_delete', {'id': tag.id}) }}" method="POST" onsubmit="return confirm('Are you sure?')">
                            <input type="hidden" name="_method" value="DELETE">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id) }}">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">No records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="navigation d-flex justify-content-end">
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}
